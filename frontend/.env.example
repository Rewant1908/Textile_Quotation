# Copy this file to .env and fill in your values.
# Never commit .env — it is in .gitignore.

# Local development: leave blank (Vite proxy in vite.config.js handles /api routing)
# Production (Railway/Vercel): set to your backend Railway URL, e.g.:
# VITE_API_URL=https://your-backend.up.railway.app
VITE_API_URL=
