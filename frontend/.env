# Local dev — points directly at the Express backend on port 5000
# Works with Live Server, file://, or any static server (no Vite proxy needed)
# For production: change this to your deployed backend URL
VITE_API_URL=http://localhost:5000
